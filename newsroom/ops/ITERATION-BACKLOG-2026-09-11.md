# neican.ai 迭代清单 · 2026-09-11

> 前置阅读：`GOVERNANCE-REVIEW-2026-09-11.md`（现状事实与红线对照）。
> 本文件只回答"接下来做什么、怎么算做完"。
> **分层原则**：P0 = 决定"内容能不能到读者"；P1 = 不改架构、天天见效的质量项；P2 = 需要新能力。
> 每项都给**验收标准**（可复验）与**依赖**，不写"优化一下"这种话。

---

## P0 · 阻断层（内容到不了读者）

| # | 事项 | 验收标准 | 依赖 |
|---|---|---|---|
| P0.1 | **停群晖 n8n**（`n8nio-n8n-1-2`） | 24h 内 main 不再出现「流水班/早报」外部提交 | 你（无群晖入口） |
| P0.2 | **撤销旧 PAT** `ghp_****Jj8W` | GitHub PAT 列表中该 token 消失；`D:/Work/n8n/hugo_sync_workflow.json` 内的串作废 | 你（GitHub 无撤销 API） |
| P0.3 | **NAS 细粒度 token**（`Contents: Read and write`，仅此一库） | 写入 `git-credentials` 后，`emit.sh` 的 `pushed` 变为 `1`，且 `commits_ahead_of_origin` 为具体数字（不再是 `-1`） | P0.1（顺序不可颠倒） |
| P0.4 | **门禁前移**：`emit.sh` commit 前跑 `validate.py` | 构造一篇违规内容，`emit.sh` **拒绝提交**并留下 `rc≠0` 记录；正常内容不受影响 | 无（可立即做） |
| P0.5 | **钉钉 webhook** 填进 `watchdog.conf` | `watchdog.py --force-alert` 的告警体能真实送达群；阶段 2.4 验收完成 | 你 |
| P0.6 | **修 P8（CF 缓存）** | `curl -sI https://www.neican.ai/index.xml` 的 `cf-cache-status` 不再长时间 `HIT`；部署后 1h 内线上可见新条目 | 你（CF 控制台）或 CF API Token |

**P0.4 的原理**：现在门禁挂在 CI 上，而发布路径是 push → Vercel（见复盘 §1.2）。
把校验搬到 NAS 的 commit 之前，门禁才真正长在链上。

---

## P1 · 质量层（不改架构，每天见效）

| # | 事项 | 证据 | 验收标准 |
|---|---|---|---|
| P1.1 | **摘要开头重复标题** | 今日日报 item 1：标题「AI移动应用2026年7月洞察：月活重回增长…」而摘要以「**活跃人数：月活5.77亿，7月重回增长** 2026年7月，AI应用行业月活5.77亿…」开头 —— 前 20 字是同一信息的重复 | 摘要与标题的公共前缀（≥8 字）被剥离；三种样本（中文重复 / 英文不重复 / 中文正常）复验通过 |
| P1.2 | **一手源占比** | 今日日报「信源分布：专业媒体 5 条」，**一手源 0 条**。而 `scoring.yaml` 给一手源的 weight 最高 —— 规则与产出不一致 | 连续 3 期日报中至少 1 条来自 `tier=first_party`，或在 `plan-*.json` 里记录"当日一手源候选数"以证明是"确实没有"而非"没选上" |
| P1.3 | **tags 规范化 / 抑制一次性 tag** | 存量 16,237 个标签中 **67.2% 只含 1 篇**；新产线的 tags 来自实体闭集，但"AI Agent"这类通用词会持续累积 | 新产出的 tags 全部落在闭集内，且不产生"只含 1 篇"的新 tag（抽验近 7 天） |
| P1.4 | **config 指纹 + repo HEAD 落盘** | `plan-2026-09-11.json` 只有 `generated_at`，无 config 版本 | `plan-*.json` 含 `config_sha` 与 `repo_head`；改一次 `scoring.yaml` 重跑，能看出结果变化可归因 |
| P1.5 | **保留策略** | `raw/` 3.2 MB/天、`state/` 814 KB/天，目前**无任何清理** | 有显式策略（建议 raw 90 天 / state 1 年）并在 `pipeline.sh` 里落地；`health.sh` 报出各自占用 |
| P1.6 | **提交身份区分** | 流水线提交与人工/agent 提交都是 `neican-bot`，审计分不清 | 流水线用 `neican-bot`，人工/agent 用另一身份或加 `Produced-By:` trailer；`git log` 可一眼区分 |

---

## P2 · 深化层（需要新能力或新决策）

| # | 事项 | 为什么要做 | 完成标志 |
|---|---|---|---|
| P2.1 | **Event 跨天去重负反馈** | 目前只做同日归并，同一事件**跨天会重复入选**（`state/events-*.jsonl` 已在落盘，接线即可） | 早报连续 7 天内不出现同一 Event；漏报率有记录 |
| P2.2 | **`novelty` 首跑偏差** | 无历史 Event 可比时系统性偏高，首跑已主动 WARN | 以 `events-*.jsonl` 历史为基线后，`novelty` 分布稳定（不再需要 WARN） |
| P2.3 | **生成层（LLM）接入** | 模板轨零创作，做不出"主编综述"，也做不出洞察成稿。日报页脚目前明确写着"主编综述需生成层启用后补入" | 日报含一段综述；模型分配按设计共识（DeepSeek 深度稿 + 另一家交叉审校）；**成本可观测** |
| P2.4 | **AIHOT 使用边界的合规确认** | 现按产品形态拆分（索引型可用改写摘要+署名；成稿型只取线索），未获你确认 | 你拍板后写进 `RUNBOOK.md` 红线；若收紧为"只用标题、摘要留空"，早报会有 ~4 条只有标题 |
| P2.5 | **发布闸归属的架构决策** | 复盘 §1.2 的两条路（门禁前移 / Vercel 只部署被验证的提交） | 选定后写进 `PLAN` 的架构决策记录（D 段） |

---

## 建议的下一个动作

**P0.1（停群晖）+ P0.4（门禁前移）**。

前者只能你做，且卡着 P0.3；后者我现在就能做，且**不等任何人**。
两者合起来的收益是：链路从"三段通、一段断、一段滞后"变成
**"写入侧自带门禁、发布侧只差一个凭据"**。

---

## 附：本清单的取舍记录

**刻意不做**：

- 不改文件命名去规避"同日双份" —— 那是把冲突藏起来。正解是退役旧写入者（P0.1）。
- 不给 `emit.sh` 加自动重试/自愈 —— 设计共识是"先告警，后自愈"，
  而告警通道还没配（P0.5）。**在没有告警之前上自愈，等于把静默放大**。
- 不引入 `flock` 之外的分布式锁 —— 单一写入者是目标，不是要克服的约束。
